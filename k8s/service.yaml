apiVersion: v1
kind: Service
metadata:
  name: devops-api-service
  namespace: default
  labels:
    app: devops-api
spec:
  type: NodePort
  selector:
    app: devops-api
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 3000
    nodePort: 30000
